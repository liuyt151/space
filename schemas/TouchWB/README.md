# TouchWB 五笔输入法方案功能说明

> 版本：v2.0
> 原作者：Shawx
> 维护者：Liuyt151
> 更新日期：2026-05-20

---

## Liuyt151 贡献说明

本项目由 **Shawx** 基于空山明月方案底包创建，**Liuyt151** 在此基础上进行了以下优化与功能扩展：

### 布局与词库优化

- **14键拇指键盘布局**：专为手机端设计，Z键独立，适合拇指击键，26键与14键均适配横屏分屏布局
- **14键专用音辅版字典**：通过第5位音辅码有效降低重码率，提升拇指输入效率

### 功能模块优化与新增

| 模块 | 说明 |
|------|------|
| smart_commit.lua | 智能上屏处理器，修复退格键误触发自动上屏问题 |
| new_spelling.lua | 字根拆分显示模块，自动适配纯五笔/五笔拼音混输方案 |
| input_statistics.lua | 输入统计模块，支持日/周/月/年多维度统计与临时统计 |
| email_suffix_translator.lua | 邮箱后缀智能补全，输入@自动提示常用邮箱域名 |

---

## 一、方案概述

TouchWB 是一款基于触宝输入法样式的五笔输入法方案，设计14键拇指键盘布局，Z键独立，适合拇指击键。方案支持纯五笔、五笔拼音混合输入等多种模式，配合14键专用音辅版字典可有效降低重码。

### 支持的输入方案

| 方案ID | 方案名称 | 说明 |
|--------|----------|------|
| wubi | 纯净五笔 | 纯五笔输入模式 |
| wubi_pinyin_mix | 笔音双混 | 五笔拼音混合输入模式 |
| wubi_14 | 拇指五笔 | 14键拇指键盘纯五笔 |
| wubi_pinyin_mix_14 | 拇指笔音双混 | 14键拇指键盘五笔拼音混合 |

---

## 二、核心功能模块

### 1. 五笔拼音混合输入

**功能描述**：在五笔的基础上支持拼音输出，实现形码、音码混打。

**实现文件**：`wubi_pinyin_mix.schema.yaml`

**特性**：
- 五笔编码直接输入汉字
- 拼音输入自动反查五笔编码
- 智能识别输入模式（五笔/拼音）
- 支持首字母缩写输入

---

### 2. 智能上屏处理器 (smart_commit.lua)

**功能描述**：智能判断五笔和拼音输入，五笔四码自动上屏，拼音保持候选状态。

**实现文件**：`lua/smart_commit.lua`

**判断逻辑**：
- 五笔模式：输入长度=4，第一个候选是 table 类型（五笔字典匹配），单字自动上屏
- 拼音模式：候选来自 reverse_lookup 类型，保持候选状态不自动上屏
- 修复退格键触发自动上屏的问题

---

### 3. 手工调频 (super_sequence.lua)

**功能描述**：支持候选项手动排序，数据存放在 lua/sequence.userdb。

**实现文件**：`lua/super_sequence.lua`

**快捷键**：
| 快捷键 | 功能 |
|--------|------|
| Ctrl+J | 将当前候选项前移一位 |
| Ctrl+K | 将当前候选项后移一位 |
| Ctrl+L | 重置排序（恢复原始顺序） |
| Ctrl+P | 将当前候选项置顶 |

**命令**：
- `/txql` - 清除所有调序记录

**特性**：
- 完全脱离 wanxiang.lua 依赖，形成独立模块
- 支持跨方案共享调序数据

---

### 4. 自造词功能 (Submit_text.lua)

**功能描述**：使用 ` 键引导组合输入，自动生成4位编码，即时生效无需重新部署。

**实现文件**：`lua/Submit_text.lua`

**使用方法**：
1. 输入 ` 键引导
2. 依次输入每个字的五笔编码（用 ` 分隔）
3. 选择候选词上屏
4. 系统自动生成4位编码并保存

**编码规则**：
- 2字词：每字取前2位编码
- 3字词：第1字1位，第2字1位，第3字2位
- 4字及以上：前3字各1位，最后1字1位

**数据存储**：`user_coined_ext.txt`

---

### 5. 计算器功能 (calculator.lua)

**功能描述**：支持数学表达式计算，输出计算结果。

**实现文件**：`lua/calculator.lua`

**使用方法**：输入 `=` 开头的数学表达式

**示例**：
- `=1+1` → 输出 `2`
- `=sin(3.14)` → 输出计算结果
- `=sqrt(16)` → 输出 `4`

**支持的函数**：
- 三角函数：sin, cos, tan, asin, acos, atan, rad, deg
- 基础数学：abs, floor, ceil, sqrt, exp, ln, log
- 数学常量：pi, e, inf
- 统计函数：min, max, sum, avg
- 函数式编程：map, filter, foldr, foldl, chain
- 微积分：deriv（导数）, integ（积分）, rk4（龙格库塔）
- 中文数字转换：speakMoneyUpper（大写金额）、speakOfficiallyLower（小写数字）

**特性**：
- 支持小键盘数字键计算
- 输出大写金额、小写金额、大写数字、小写数字等多种格式

---

### 6. 日程模块 (schedule.lua)

**功能描述**：支持时间显示、节日倒计时、节气提醒及农历信息。

**实现文件**：`lua/schedule.lua`

**命令列表**：
| 命令 | 功能 |
|------|------|
| /jc | 显示完整日程信息（问候语、日期、农历、年进度、节日倒计时、节气倒计时） |
| /jq | 显示二十四节气信息 |
| /gz | 显示干支历信息 |
| /date | 显示农历日期转换信息 |

**输出内容**：
- 问候语（根据时间段自动变化）
- 公历日期、星期、周数
- 农历日期、生肖、节气
- 年进度百分比
- 距离下一个农历新年的天数
- 最近的节日倒计时
- 最近的节气倒计时

---

### 7. 超级提示 (super_tips.lua)

**功能描述**：根据输入或候选项显示提示信息，支持表情、化学式、方程式、简码等直接上屏。

**实现文件**：`lua/super_tips.lua`

**开关**：`关提 / 开提`

**上屏键**：`/` 键（可配置）

**特性**：
- 采用 LevelDB 数据库存储
- 支持候选匹配和编码匹配两种模式
- 支持方向键高亮遍历
- 数据文件：`lua/tips/tips_show.txt`

---

### 8. 智能标点配对 (pair_punct.lua)

**功能描述**：自动补齐成对符号，输入左符号自动补全右符号。

**实现文件**：`lua/pair_punct.lua`

**开关**：`关配 / 开配`

**支持的配对符号**：
- 引号："" 、''
- 括号：() 、[] 、{} 、<> 、（） 、【】 、〔〕
- 书名号：《》 、〈〉 、「」 、『』
- 其他：`` 、〖〗 、〚〛 、〘〙

**特性**：
- 支持单双引号直接成对输出
- 支持回车键上屏配对符号
- 修复符号可按回车上屏问题

---

### 9. 中英文互译 (english_translator.lua + translate_merge.lua)

**功能描述**：输入英文单词显示中文翻译，支持前缀匹配。

**实现文件**：
- `lua/english_translator.lua` - 翻译器
- `lua/translate_merge.lua` - 合并过滤器
- `opencc/english_chinese.txt` - 英汉词典
- `opencc/chinese_english.txt` - 汉英词典

**开关**：`关译 / 开译`

**特性**：
- 支持大小写不敏感匹配
- 支持前缀匹配（输入部分字母提示完整单词）
- 优化翻译开关逻辑，译关状态下零查询开销
- 自动清空翻译字典缓存，最小化内存占用
- 过滤五笔编码产生的无关词汇

---

### 10. 字根拆分显示 (new_spelling.lua)

**功能描述**：显示汉字的字根拆分、拼音、编码等注解信息。

**实现文件**：`lua/new_spelling.lua`

**相关开关**：
| 开关 | 功能 |
|------|------|
| 显码 / 隐码 | 显示/隐藏五笔字型编码 |
| 隐根 / 显根 | 显示/隐藏汉字部件拆分（字根） |
| 显拼 / 隐拼 | 显示/隐藏单字带声调拼音 |
| 隐声 / 显声 | 词组拼音/字根拆分切换（仅混输有效） |

**特性**：
- 自动适配纯五笔/五笔拼音混输方案
- 支持单字和词组的字根拆分显示
- 支持 GB2312 字符集过滤

---

### 11. 拼音反查

**功能描述**：按 z 键后输入拼音，显示对应的五笔编码。

**使用方法**：
1. 按 `z` 键进入拼音反查模式
2. 输入拼音（如 `zhong`）
3. 候选项显示汉字及其五笔编码

**特性**：
- 支持带声调拼音显示
- 显示字根拆分信息

---

### 12. 输入统计 (input_statistics.lua)

**功能描述**：统计输入数据，支持日、周、月、年多维度统计。

**实现文件**：`lua/input_statistics.lua`

**命令列表**：
| 命令 | 功能 |
|------|------|
| /tj 或 tjxs | 显示统计报告 |
| /qk 或 tjqk | 清空统计数据（保留昨日数据） |
| /ks 或 tjks | 开始临时统计会话 |
| /js 或 tjjs | 结束临时统计并生成报告 |
| /tc 或 tjtc | 退出临时统计（不生成报告） |

**统计内容**：
- 上屏次数、总字数
- 极速（基于最近10秒瞬时速度）
- 均速（基于总字数/总连续输入时间）
- 与昨日对比
- 按方案分开存储

**特性**：
- 自动保存统计数据，重启后不丢失
- 5秒间隙自动切断连续输入段
- 自动备份（.bak文件）

---

### 13. 邮箱后缀补全 (email_suffix_translator.lua)

**功能描述**：输入邮箱时自动显示常用邮箱后缀。

**实现文件**：`lua/email_suffix_translator.lua`

**触发方式**：输入 `@` 后自动显示邮箱后缀候选

---

### 14. 符号快捷输入 (symbols.yaml)

**功能描述**：通过 `/` 键引导快速输入特殊符号。

**实现文件**：`symbols.yaml`

**使用方法**：输入 `/` + 字母或数字

**示例**：
- `/tg` → 显示天干：甲、乙、丙、丁、戊、己、庚、辛、壬、癸
- `/dz` → 显示地支
- `/1` - `/10` → 输出各种数字符号

---

## 三、功能开关

### 开关列表

| 开关名称 | 状态 | 功能说明 |
|----------|------|----------|
| 显码 / 隐码 | 默认显码 | 显示/隐藏五笔字型编码（单字） |
| 隐根 / 显根 | 默认显根 | 显示/隐藏汉字部件拆分（字根） |
| 显拼 / 隐拼 | 默认显拼 | 显示/隐藏单字带声调拼音 |
| 隐声 / 显声 | 默认隐声 | 词组拼音/字根拆分切换（仅混输） |
| 关配 / 开配 | 默认关配 | 配对标点（括号、引号等自动成对） |
| 关提 / 开提 | 默认关提 | 超级提示 |
| 关译 / 开译 | 默认关译 | 中英文互译 |
| 扩展 / 常用 | 默认扩展 | 常用字符集（GB2312）过滤 |
| 隐表 / 显表 | 默认隐表 | Emoji 表情 |
| 简体 / 繁体 | 默认简体 | 简繁转换 |
| 。，/ ．， | 默认全角 | 标点符号全角/半角 |
| 半角 / 全角 | 默认半角 | 字符全角/半角 |

---

## 四、快捷键绑定

### 功能切换快捷键

| 快捷键 | 功能 |
|--------|------|
| Ctrl+Shift+F | 切换简繁体 |
| Ctrl+Shift+H | 切换字根显示 |
| Ctrl+Shift+J | 切换拼音显示 |
| Ctrl+Shift+K | 切换编码显示 |
| Ctrl+Shift+E | 切换 Emoji 显示 |
| Ctrl+Shift+U | 切换字符集 |
| Ctrl+Shift+T | 切换中英转换 |
| Ctrl+Shift+P | 切换配对标点开关 |
| Shift+空格 | 切换全半角 |
| Ctrl+. | 切换标点全半角 |

### 候选项操作快捷键

| 快捷键 | 功能 |
|--------|------|
| ; (分号) | 上屏第二候选 |
| ' (引号) | 上屏第三候选 |
| - (减号) | 向上翻页 |
| = (等号) | 向下翻页 |
| ← (左箭头) | 在候选项中向上移动 |
| → (右箭头) | 在候选项中向下移动 |

### 手工调频快捷键

| 快捷键 | 功能 |
|--------|------|
| Ctrl+J | 候选项前移 |
| Ctrl+K | 候选项后移 |
| Ctrl+L | 重置排序 |
| Ctrl+P | 候选项置顶 |

### Emacs 风格编辑键

| 快捷键 | 功能 |
|--------|------|
| Ctrl+P | 向上移动/候选词上移 |
| Ctrl+N | 向下移动/候选词下移 |
| Ctrl+B | 向左移动光标 |
| Ctrl+F | 向右移动光标 |
| Ctrl+A | 移动到行首 |
| Ctrl+E | 移动到行尾 |
| Ctrl+D | 删除光标后字符 |
| Ctrl+K | 删除到行尾 |
| Ctrl+H | 退格 |
| Ctrl+G | 取消/退出 |
| Ctrl+[ | 取消（Vim风格） |
| Alt+V | 向上翻页 |
| Ctrl+V | 向下翻页 |

---

## 五、主题支持

### 主题列表

| 主题目录 | 适用方案 | 说明 |
|----------|----------|------|
| themes/t26_wubi | wubi, wubi_pinyin_mix, pinyin | 26键标准键盘主题 |
| themes/t14_wubi | wubi_14, wubi_pinyin_mix_14 | 14键拇指键盘主题 |

### 主题资源

每个主题包含以下资源文件：
- `ch_en.svg` - 中英文切换图标
- `en_ch.svg` - 英中文切换图标
- `number.svg` - 数字键盘图标
- `symbols.svg` - 符号键盘图标

---

## 六、词库说明

### 主词库

| 词库文件 | 说明 |
|----------|------|
| wubi.dict.yaml | 五笔主词库 |
| wubi.extended.dict.yaml | 五笔扩展词库 |
| wubi_14.dict.yaml | 14键五笔词库 |
| pinyin.dict.yaml | 拼音词库 |
| wb_spelling.dict.yaml | 字根拆分数库 |

### 扩展词库

- 整合知频词库筛选的2W多词
- 2-4字词条分别整合到五笔与拼音词典
- 适配空山方案词序调整

---

## 七、文件结构

```
TouchWB/
├── icons/                          # 状态栏图标
│   ├── CN.ico                      # 中文状态图标
│   ├── EN.ico                      # 英文状态图标
│   ├── full.ico                    # 全角状态图标
│   └── half.ico                    # 半角状态图标
├── lua/                            # Lua 脚本目录
│   ├── lib/                        # 库文件
│   │   ├── basic.lua               # 基础工具库
│   │   ├── bit.lua                 # 位运算库
│   │   ├── lunar.lua               # 农历计算库
│   │   └── userdb.lua              # 用户数据库库
│   ├── tips/                       # 超级提示数据
│   │   └── tips_show.txt           # 提示数据文件
│   ├── Submit_text.lua             # 自造词模块
│   ├── calculator.lua              # 计算器模块
│   ├── email_suffix_translator.lua # 邮箱后缀补全
│   ├── english_translator.lua      # 英文翻译器
│   ├── input_statistics.lua        # 输入统计模块
│   ├── new_spelling.lua            # 字根拆分显示
│   ├── pair_punct.lua              # 配对标点模块
│   ├── schedule.lua                # 日程模块
│   ├── smart_commit.lua            # 智能上屏处理器
│   ├── super_sequence.lua          # 手工调频模块
│   ├── super_tips.lua              # 超级提示模块
│   ├── translate_merge.lua         # 翻译合并过滤器
│   └── wanxiang.lua                # 万象基础模块
├── opencc/                         # OpenCC 转换词典
│   ├── ce.json                     # 中英转换配置
│   ├── chinese_english.txt         # 汉英词典
│   ├── emoji.json                  # Emoji 转换配置
│   ├── emoji.txt                   # Emoji 数据
│   ├── emoji_category.txt          # Emoji 分类
│   ├── emoji_word.txt              # Emoji 词汇
│   ├── english_chinese.txt         # 英汉词典
│   ├── es.json                     # Emoji 转换配置
│   └── es.txt                      # Emoji 数据
├── themes/                         # 主题目录
│   ├── public/                     # 公共主题配置
│   │   ├── info/                   # 方案信息
│   │   ├── preset_candidates/      # 预设候选
│   │   ├── preset_color_schemes/   # 颜色方案
│   │   ├── preset_fonts/           # 字体配置
│   │   ├── preset_keyboards/       # 键盘布局
│   │   ├── preset_keys/            # 按键配置
│   │   ├── preset_menu/            # 菜单配置
│   │   ├── preset_styles/          # 样式配置
│   │   └── style/                  # 样式文件
│   ├── t14_wubi/                   # 14键主题
│   │   ├── res/                    # 主题资源
│   │   ├── info.yaml               # 主题信息
│   │   └── theme.yaml              # 主题配置
│   └── t26_wubi/                   # 26键主题
│       ├── res/                    # 主题资源
│       ├── info.yaml               # 主题信息
│       └── theme.yaml              # 主题配置
├── default.custom.yaml             # 默认配置定制
├── default.yaml                    # 默认配置
├── info.yaml                       # 方案信息
├── key_bindings.yaml               # 按键绑定配置
├── pinyin.dict.yaml                # 拼音词库
├── pinyin.schema.yaml              # 拼音方案
├── symbols.yaml                    # 符号配置
├── wb_spelling.dict.yaml           # 字根拆分词库
├── wb_spelling.schema.yaml         # 字根拆分方案
├── weasel.custom.yaml              # 小狼毫定制配置
├── weasel.yaml                     # 小狼毫配置
├── wubi.dict.yaml                  # 五笔词库
├── wubi.extended.dict.yaml         # 五笔扩展词库
├── wubi.schema.yaml                # 纯五笔方案
├── wubi_14.dict.yaml               # 14键五笔词库
├── wubi_14.schema.yaml             # 14键五笔方案
├── wubi_pinyin_mix.schema.yaml     # 五笔拼音混合方案
├── wubi_pinyin_mix_14.schema.yaml  # 14键五笔拼音混合方案
└── 修改说明.txt                    # 版本更新记录
```

---

## 八、依赖说明

### Rime 版本
- Rime 版本：1.13.1
- 小狼毫版本：0.17.4

### 依赖方案
- wb_spelling（字根拆分数库）
- pinyin（拼音词库）
- wubi（五笔词库）

---

## 九、致谢

本项目由 **Shawx** 基于空山明月方案底包修改制作，**Liuyt151** 在此基础上进行了持续优化、功能增强与模块扩展。
- **空山明月**：底包方案
- **万象拼音方案**：super_sequence.lua、super_tips.lua
- **知频词库**：扩展词库来源
- **Shawx**：项目创建与架构设计，核心模块开发（new_spelling、smart_commit、Submit_text、pair_punct、calculator、english_translator、translate_merge 等）
- **Liuyt151**：功能优化与问题修复，新增模块（input_statistics、email_suffix_translator 等），14键拇指布局适配，词库扩展与维护
- 所有贡献者和测试者

---

*流萤*
*流光一瞬，不羡永恒。*
*萤火虽微，自照前程。*
